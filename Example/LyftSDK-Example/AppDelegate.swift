import CoreLocation
import LyftSDK
import UIKit

@UIApplicationMain
class AppDelegate: UIResponder, UIApplicationDelegate {
    var window: UIWindow?

    func applicationDidFinishLaunching(_ application: UIApplication) {
        /* Uncomment and insert your designated client token and identifier. https://www.lyft.com/developers
        let token = "..."
        let clientId = "..."
        LyftConfiguration.developer = (token: token, clientId: clientId)
        */
    }
}
